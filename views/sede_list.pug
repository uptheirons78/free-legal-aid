extends layout

block content

  br
  h1.page_title= title
  br
  .container
    .row
      .col-md-offset-3.col-md-6.text-center
        each sede in sede_list
          h4
            a(href=sede.url) #{sede.nome}

        else
          p Non ci sono al momento sedi di giudizio.