extends layout

block content

  br
  h1.page_title= title
  br
  .container
    .row
      .col-md-offset-3.col-md-6.text-center
        each materia in materia_list
          h4
            a(href=materia.url) #{materia.nome}

        else
          p Non ci sono al momento materie di giudizio.